import Drawable from 'App/AbstractObjects/Drawable'

class SceneBorder extends Drawable {

    constructor(image: HTMLImageElement) {
        super(image)
    }

    public draw(context: CanvasRenderingContext2D) {
    }
}

export default SceneBorder